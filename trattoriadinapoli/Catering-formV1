<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>Catering Request — Compact</title>
<style>
  :root{
    --bg:#e2ddd1;
    --card:#fffbe6;
    --muted:#f7f7f7;
    --shadow: 0 2px 5px rgba(0,0,0,0.08);
    --input-border: rgba(0,0,0,0.12);
    --radius:8px;
  }

  html,body{margin:0;font-family:system-ui,-apple-system,Segoe UI,Roboto,"Helvetica Neue",Arial;}
  body{background:var(--bg); padding:28px; box-sizing:border-box;}

  .container{
    margin:0 auto;
    background:transparent;
  }

  form#catering-form{
    display:flex; flex-direction:column; gap:16px;
    background:transparent;
  }

  .top-grid{ display:grid; grid-template-columns: repeat(auto-fit, minmax(180px, 1fr)); gap:12px; }

  label.field-label{ font-size:15px; margin-bottom:6px; display:block; font-weight:600; color:#111; }

  input[type="text"], input[type="email"], input[type="tel"], input[type="number"], textarea,
  input#deliveryAddress, input#eventDate {
    width:100%;
    padding:10px;
    border-radius:6px;
    border:1px solid var(--input-border);
    box-sizing:border-box;
    font-size:18px;
  }

  select { padding:8px; border-radius:6px; border:1px solid var(--input-border); font-size:18px; }

  small.note { display:block; margin-top:6px; color:#444; font-size:14px; }

  h3.section-title{ margin:6px 0 4px 0; font-size:22px; color:#0b0b0b; }

  /* MENU: compact + layout adjustments */
  .menu-section{ margin-top:6px; }
  .menu-section h4{ margin:8px 0; font-size:16px; color:#111; border-bottom:1px solid rgba(0,0,0,0.06); padding-bottom:6px; }

  .menu-grid{
    display:grid;
    grid-template-columns: repeat(auto-fit, minmax(170px, 1fr));
    gap:8px;
  }

  .menu-item{
    background:var(--card);
    padding:8px;
    border-radius:6px;
    box-shadow:var(--shadow);
    display:flex;
    flex-direction:column;
    gap:6px;
    font-size:15px;
  }

  .menu-item .top-line{ display:flex; align-items:center; gap:8px; flex-wrap:wrap; }
  .menu-item .top-line .item-name{ 
    font-size:18px;
    font-weight:700;
  }

  .menu-options{
    display:flex;
    gap:8px;
    align-items:center;
    flex-wrap:wrap;
  }

  .menu-options .menu-size,
  .menu-options .menu-price,
  .menu-options input.menu-qty,
  .menu-options .size-text{
    font-size:18px;
  }

  .menu-options .menu-price{
    min-width:70px;
    text-align:right;
    font-weight:600;
  }

  .menu-options input.menu-qty{
    width:64px;
    padding:6px;
    border-radius:6px;
    border:1px solid var(--input-border);
    font-size:18px;
  }

  .menu-options .size-text{
    min-width:90px;
    font-weight:500;
  }

  .bottom-grid {
    display: flex;
    gap: 16px;
    margin-top: 20px;
    align-items: stretch;
  }
  .bottom-left, .bottom-right {
    border-radius: 8px;
    padding: 12px;
    box-shadow: 0 2px 5px rgba(0,0,0,0.08);
    background: white;
  }
  .bottom-left { flex: 1 1 40%; }
  .bottom-right {
    flex: 1 1 60%;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
  }
  .fineprint {
    background: #f7f7f7;
    padding: 10px;
    border-radius: 6px;
    font-size: 16px;
    color: #333;
    flex: 1;
  }
  button[type="submit"] {
    padding: 10px;
    font-size: 17px;
    background: black;
    color: white;
    border: none;
    border-radius: 6px;
    cursor: pointer;
    width: 100%;
  }

  /* === Error styles === */
  .error-message {
    color: #c00;
    font-size: 14px;
    margin-top: 4px;
    display: block;
  }

  input.invalid {
    border-color: #c00;
    background: #fff0f0;
  }

  input:focus.invalid {
    outline: 2px solid #c00;
  }

  @media (max-width:719px){
    .bottom-grid{ flex-direction:column; }
    .bottom-left, .bottom-right{ min-width:unset; width:100%; }
    .menu-grid{ grid-template-columns: repeat(auto-fit, minmax(140px,1fr)); }
  }
</style>
</head>
<body>
  <div class="container">
    <form id="catering-form" action="https://formspree.io/f/mblzpbzq" method="POST">
      <!-- Top Contact & Event Grid -->
      <div class="top-grid">
        <div>
          <label class="field-label" for="name">Full Name *</label>
          <input id="name" name="name" required type="text" placeholder="Full Name">
          <span class="error-message" id="err-name"></span>
        </div>
        <div>
          <label class="field-label" for="email">Email *</label>
          <input id="email" name="email" required type="email" placeholder="Email">
          <span class="error-message" id="err-email"></span>
        </div>
        <div>
          <label class="field-label" for="phone">Phone *</label>
          <input id="phone" name="phone" required type="tel" placeholder="Phone">
          <span class="error-message" id="err-phone"></span>
        </div>
        <div>
          <label class="field-label" for="people">Number of People *</label>
          <input id="people" name="people" required type="number" min="1" placeholder="People">
          <span class="error-message" id="err-people"></span>
        </div>
      </div>

      <!-- Date + Confirmation -->
      <div class="top-grid">
        <div>
          <label class="field-label" for="eventDate">Event Date *</label>
          <input id="eventDate" name="eventDate" required type="date">
          <span class="error-message" id="err-eventDate"></span>
          <small class="note">*(Orders must be placed at least two weeks in advance.)</small>
        </div>

        <div>
          <label class="field-label">Preferred Confirmation Days *</label>
          <div style="display:flex; gap:8px; flex-wrap:wrap; margin-top:6px;">
            <label><input type="checkbox" name="confirmationDays" value="Tuesday"> Tue</label>
            <label><input type="checkbox" name="confirmationDays" value="Wednesday"> Wed</label>
            <label><input type="checkbox" name="confirmationDays" value="Thursday"> Thu</label>
            <label><input type="checkbox" name="confirmationDays" value="Friday"> Fri</label>
            <label><input type="checkbox" name="confirmationDays" value="Saturday"> Sat</label>
          </div>
          <span class="error-message" id="err-timeframe"></span>
        </div>

        <div>
          <label class="field-label">Preferred Timeframe *</label>
          <div style="display:flex; gap:8px; margin-top:6px;">
            <select id="startTime" required>
              <option value="">Start</option>
            </select>
            <select id="endTime" required>
              <option value="">End</option>
            </select>
          </div>
        </div>
      </div>

      <!-- Pickup/Delivery -->
      <div class="top-grid" style="align-items:end;">
        <div>
          <label class="field-label">Pickup / Delivery *</label>
          <div style="display:flex; gap:8px; margin-top:6px;">
            <button type="button" id="togglePickup" aria-pressed="true" style="flex:1; border-radius:6px; padding:8px; background:#000; color:#fff; border:1px solid var(--input-border);">Pickup</button>
            <button type="button" id="toggleDelivery" aria-pressed="false" style="flex:1; border-radius:6px; padding:8px; background:#fff; color:#000; border:1px solid var(--input-border);">Delivery</button>
          </div>
          <input type="hidden" name="pickupDelivery" id="pickupDeliveryInput" value="pickup">
        </div>

        <div id="deliveryWrapper" style="display:none;">
          <label class="field-label" for="deliveryAddress">Delivery Address</label>
          <input id="deliveryAddress" name="deliveryAddress" placeholder="Delivery Address">
          <span class="error-message" id="err-deliveryAddress"></span>
        </div>
      </div>

      <!-- Menu Section -->
      <h3 class="section-title">Place Your Order</h3>
      <div id="menuContainer"></div>

      <!-- Totals + Submit Area -->
      <div class="bottom-grid">
        <div class="bottom-left">
          <h4 style="margin:0 0 6px 0;">Order Summary</h4>
          <div>Subtotal: $<span id="subtotal">0.00</span></div>
          <div>Service Charge (20%): $<span id="serviceCharge">0.00</span></div>
          <div><strong>Total: $<span id="totalPrice">0.00</span></strong></div>
          <div>Deposit (50%): $<span id="deposit">0.00</span></div>
          <div>Estimated Servings: <span id="estimatedPeople">0</span> people</div>
        </div>

        <div class="bottom-right">
          <div class="fineprint">
            *(50% non-refundable deposit required within 24 hours of confirmation for online booking.)<br>
            *(Remaining balance due one week before event.)<br>
            *(A 20% service charge is added to all catering orders.)<br>
            *(All unfilled portions of this form will be reviewed during confirmation.)
          </div>

          <button type="submit">Submit Catering Request</button>
        </div>
      </div>

      <textarea name="summary" id="summaryField" hidden></textarea>
    </form>
  </div>

  <!-- EXTERNAL SCRIPT -->
  <script src="https://stucowebsolutions.github.io/clientsolutions/trattoriadinapoli/catering-form.js"></script>
</body>
</html>
